from __future__ import annotations

from rag.config import Settings


def configure_observability(settings: Settings | None = None) -> bool:
    """Temporary no-op stub; the real Langfuse wiring lands in Task 12."""
    return False
